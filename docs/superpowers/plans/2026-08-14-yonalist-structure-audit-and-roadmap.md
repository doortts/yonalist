# Yonalist 구조 진단과 리팩터링 로드맵 (핸드오프)

- 작성: 2026-08-14. 2차 개정: 같은 날, 적대적 리뷰 반영. 기준: `main@8c091628`, 작업 트리 clean.
- 이 문서 하나가 재개 지점의 진실 소스다. 세션 채팅과 실행 중 태스크는 기기 전환 시 소실된다.
- 근거: 코드 감사 3건(프런트엔드 / Rust·Tauri 백엔드 / 문서-실제 대조, 전부 file:line 인용) + 제품 조사 2건(Workflowy 2025–26 기능·성능, Logseq DB 전환 사후 분석) + 그 감사에 대한 적대적 재검증 1건.
- 실행 체제: 설계·리뷰 Fable xHigh, 코드 Opus xHigh, 항목당 1커밋, 단계마다 적대적 리뷰 후 다음 항목.
- §9 실행 기록이 P1 진행 상황을 담는다. 재개할 때 §0 다음으로 볼 곳이다.

## 0. 재개 지점

**기준 커밋 `main@8c091628`, 작업 트리 clean.** 1차 문서가 "커밋 안 됨"이라 기록했던 두 갈래는 전부 커밋됐다.

| 갈래 | 커밋 |
|---|---|
| macOS 네이티브 창 크롬 (Rust `WebviewWindowBuilder`, 신호등 인셋 버전 분기) + P0-1 권한 한 줄 | `aa01130b` |
| 사이드바 재편 (검색이 헤더 아이콘 뒤로 접힘) | `2942c525` |
| `#` 태그 스펙 | `8c091628` |

> 교훈: 작업 트리 스냅샷을 로드맵 문서에 박으면 그날 안에 낡는다. 이 문서는 커밋된 사실만 기록하고, 진행 상황은 §9에만 쓴다.

**완료: P0-1.** `scripts/checkV2Architecture.mjs:192`의 하드코딩 권한 목록에 `core:window:allow-start-dragging`을 넣었다. 창 크롬 작업이 이 권한을 capability에 추가했는데 검사 목록에 없어 `npm run test:v2`가 통째로 실패하던 상태였다.

**P1 완료** (2026-08-14). 7개 항목 전부 착지 — §9에 항목별 결과와 측정값. 요약:

| | 결과 |
|---|---|
| 커밋 | `b9a4c3f4` `bd0eb2ad` `9b068007` `6d0aae3c` `9ed3d455` `78a1a9e4` (브랜치 `refactor/p1-structure-surgery`) + `958f8b20` (브랜치 `refactor/p1-9-folder-structure`, **머지 대기**) |
| 성능 | 5k `plan()` 3.30s → 0.04s · 5k 서브트리 삭제 1.61s → 0.03s · 복제 1.71s → 0.07s · 같은 페이지 스크롤 쓰기 N회 → 0회 |
| 테스트 | 733 → 786 (프런트) + Rust 통합 테스트 14개 신규 |
| 계약 신설 | 심부 스크롤 (5k 마지막 창 · 50k 최심부 창) |
| 범위에서 뺀 것 | 접힌 가지 가지치기 → §8 (SQL 층에서 닫히지 않음) |
| 게이트에서 발견한 결함 2건 | 성능 스위트가 작성일부터 빨간 상태(P0-2 결정 2) · 레거시 차단 가드가 깊이 하나에서만 작동(§9 말미) |

**다음: §8 스키마 창.** 순서는 사다리 골격 → 정렬 키 물질화 → 자식 수 계약 + 가지치기. P0-3(v1 아카이브)은 SSOT 포팅 뒤 그대로.

## 1. 핵심 판정

**v2 코드베이스는 방향이 옳고 품질도 평균을 크게 웃돈다. 대규모 재작성은 필요 없다.** 순수 도메인 크레이트, 타입화된 에러, 의존 방향 자동 검사를 갖춘 구조라 리팩터링은 국소 수술로 끝난다.

급한 것은 코드가 아니라 결정 3건이다.

1. 지향점인 "텍스트 파일 = 진실 소스"가 정작 출하되는 v2에는 없다. 완성된 파일 동기화 파이프라인은 동결된 v1 안에 잠들어 있고, parity matrix에서는 "승인된 제외" 항목이다.
2. 저장소 Rust의 89%, 프런트엔드의 84%가 그 동결된 v1이다.
3. CI는 레거시 트리만 검사한다. v2 게이트는 전부 수동이다.

### 저장소 지도 — 한 저장소, 두 개의 앱

루트 `src/` + `src-tauri/`(v1)와 `apps/desktop/` + `crates/`(v2)는 계층 관계가 아니라 코드를 한 줄도 공유하지 않는 별개 애플리케이션이다. `apps/desktop/src-tauri/Cargo.toml`에 `yonalist` 의존이 없고, `checkV2Architecture.mjs`는 v2가 레거시 프런트엔드를 import하면 오류를 낸다.

| | v1 (루트) | v2 (apps/desktop + crates) |
|---|---|---|
| 역할 | 동결. parity matrix의 행동 기준으로만 참조 | 출하 제품 (`com.doortts.yonalist.v2`) |
| 규모 | TS 197,027 LOC / Rust 121,517 LOC | TS 37,578 LOC / Rust 1,550 + 15,844 LOC |
| IPC 명령 | 79개 | 20개 |
| 저장 모델 | markdown vault SSOT + SQLite 캐시 (HLC 동기화 완비) | **SQLite 단독** (`notes-v2.sqlite`) |
| Cargo | 워크스페이스에서 `exclude` (`Cargo.toml:9`) | 워크스페이스 멤버 5개 + 어댑터 |
| CI | `ci.yml`이 검사하는 유일한 대상 | 어떤 검사도 CI에 없음 |
| 마지막 커밋 | 2026-07-29 | 2026-08-14 |

기본 실행 명령(`npm run tauri:dev`)은 v2를 띄우지만 기본 `npm test` / `build` / `lint`는 여전히 v1을 가리킨다.

### v2 구조 요약

백엔드는 육각형 계층이다. `apps/desktop/src-tauri`(어댑터, IPC 20개, `StartupGate`로 비차단 부팅) → `notes-application`(포트·계약, ts-rs로 TS 타입 생성, `NotesError` 12종) → `notes-core`(순수 도메인, 의존성은 serde/thiserror뿐) → `notes-sqlite`·`notes-export`(단일 워커 스레드 + mpsc, STRICT 테이블, revision 낙관적 동시성).

프런트엔드는 라이브러리 없이 만든 저장소 클래스 + `useSyncExternalStore` 조합이다. 노드는 트리가 아니라 평탄 배열(`NoteView[]`, 부모 포인터 + `sortKey` 정수)로 두고 트리 구조는 `OutlineIndex`가 필요할 때 파생시킨다. 구독은 셸 / 아웃라인 / 노드별 3단계로 나눠 행 단위 리렌더를 막는다. 되돌리기는 Rust가 실제 스택을 소유하고 프런트는 그룹 경계와 커서 복원만 맡는다.

## 2. 지킬 것 (리팩터링에서 건드리지 말 것)

- **순환 의존 0건.** `checkV2Architecture.mjs`가 Rust·프런트 양쪽 그래프를 기계 검사한다. 죽은 모듈도 0건.
- **TODO/FIXME 0건** (13만 줄 전체 — 재검증에서 걸린 2건은 `outlineClipboard.ts`의 `TODO_BOX` 정규식 상수로, 주석이 아니다). `ponytail:` 한계 표시 8건은 전부 실제 상한을 정확히 기록한다.
- **테스트 밀도.** 프런트 1.17 : 1(테스트 LOC : 제품 LOC), v2 Rust는 통합 테스트 18파일 + 성능 계약 테스트.
- **v1의 원자적 파일 계층** — `src-tauri/src/file_io.rs`: fsync + 부모 디렉터리 sync + `renameat2(NOREPLACE)` + cap-std 샌드박스, 제품 코드 unwrap 0. 포팅 1순위 자산.
- **v1 exporter의 자가 검증** — `exporter.rs:2164`가 쓸 바이트를 다시 파싱해 불일치면 거부하고, 느린 클라우드 FS가 DB를 막지 않도록 잠금을 풀고 쓴다.
- **모범 모듈** — `apps/desktop/src/api.ts`(87줄, 메서드당 invoke 하나), `outlineKeyboard.ts`(React도 DOM도 모르는 순수 키 해석기), `outlineClipboard`/`outlinePaste`(왕복 대칭 문서화), `outlineMenuCommands.ts`(선언적 명령 테이블).
- **일관된 용어 체계** — band·head·anchor·station·forest가 전 파일에서 같은 뜻으로 쓰인다. 주석은 재서술이 아니라 근거(WebKit 특성, 타이밍 제약)를 남긴다.

## 3. 리팩터링 항목

### P0 — 게이트와 죽은 무게

| # | 항목 | 근거 | 크기 |
|---|---|---|---|
| 1 | ~~권한 목록 1줄 수리~~ **완료** (`aa01130b`) | `checkV2Architecture.mjs:192` | 1줄 |
| 2 | ~~CI를 v2로 전환~~ **완료** (`380790f3`) | v2 관련 검사 0건이었다 | S + 결정 3건 |
| 3 | **v1 처분 결정.** 권고: 태그를 남기고 브랜치로 아카이브한 뒤 main에서 삭제. 단 **실행 시점은 SSOT 포팅 완료 뒤로 미룬다**(§7 참조). 루트 `npm test`/`build`/`lint`가 v1을 가리키는 문제는 P0-2에서 먼저 해소한다. | Rust 89% · TS 84%가 동결 코드 | 결정 + S |

**P0-2 완료** (`380790f3`). 품고 있던 결정 3건과, 그 과정에서 드러난 게이트 결함들:

1. **v1 CI 잡의 처분 — 유지하되 격리.** v1은 SSOT 포팅의 원본이라 아직 지우지 않으므로 잡도 남긴다. 다만 GitHub에는 잡 단위 `paths` 필터가 없어 별도 워크플로 `ci-v1.yml`로 옮기고 v1 경로 10개에 걸었다. `ci.yml`은 v2 전용이 되었고 **필터가 전혀 없다** — 무엇도 빠져나갈 수 없다.
2. **성능 계약 잡 — PR 밖으로.** 릴리즈 프로필이 `lto = true`·`codegen-units = 1`이라 빌드가 비싸고, 단언이 벽시계라 공유 러너에서 흔들린다. main 푸시 + 수동 실행에서만 돈다. `notes-sqlite`는 GUI 크레이트에 닿지 않아(직접 확인: `cargo tree` 매치 0건) 시스템 패키지 설치도 없다.
3. **번들 예산 — 컴포짓에 넣고 천장을 정직하게.** `npm run v2:build`가 3.4초라 비용이 작다. 넣는 김에 **v2 게이트가 처음으로 타입체크를 갖게 됐다** — 번들 검사가 빌드를 선행하기 때문이다.

### 컴포짓 밖의 검사는 검사가 아니다 — 사례 4건

이 항목을 하면서 같은 패턴이 넷으로 늘었다. 전부 "초록이라 아무도 안 본 것"이 아니라 "아무도 안 돌려서 빨간 줄 몰랐던 것"이다.

| 검사 | 상태였던 것 | 언제부터 | 지금 |
|---|---|---|---|
| 성능 계약 | 7개 중 3개 실패 | `c7b17cce`(2026-07-29), 작성된 날부터 | `ce567e3a`가 픽스처 수리, 7/7. 같은 커밋이 `--release`도 붙였다(디버그로 성능을 재고 있었다) |
| 레거시 차단 가드 | 깊이 하나에서만 작동 | 서브폴더가 생기는 순간 무력화 | P1-9에서 지정자 해석 방식으로 교체 |
| 번들 예산 | **초과 상태** 331,802 raw / 100,785 gzip vs 319,488 / 98,304 | `8c091628`에서도 330,106 / 100,212 — 최근 작업 기여분은 0.5%뿐 | 컴포짓에 편입, 천장 328KB/100KB. 여유 4,070 raw / 1,615 gzip |
| **루트 vitest** | **4파일 / 6테스트 실패** | `8c091628`에서도 동일 — **이 저장소의 유일한 CI가 계속 빨갰다** | 루트 설정에서 `apps/**` 제외 |

마지막 것이 제일 나쁘다. v1 `frontend` 잡의 `npx vitest run` 단계가 v2 스위트까지 끌어다 v1 setup 폴리필 아래서 돌리고 있었다. v2 스위트는 자기 설정과 자기 setup이 따로 있고 `test:v2:frontend`가 제대로 돌린다.

거기에 하나 더 — 이 항목이 새로 넣은 워크플로 구조 테스트(`scripts/ciWorkflow.test.ts`) 자체가 **어느 게이트에도 없었다.** 컴포짓은 루트 vitest를 부르지 않고, `ci.yml`을 고쳐도 v1 필터에 걸리지 않아 워크플로를 지키라고 만든 파일이 워크플로가 바뀔 때 안 돈다. 적대적 리뷰에서 잡아 `test:v2:scripts`를 컴포짓에 넣었다(0.6초).

### 머지 전에 사람이 확인할 것

**브랜치 보호 규칙.** `frontend`나 `rust`가 required status check로 걸려 있다면, `paths` 필터 때문에 실행되지 않은 잡이 영원히 pending으로 남아 PR을 막는다. 저장소 설정을 볼 수 없어 확인하지 못했다. required check 목록을 `v2`로 바꿔야 한다.

부수 관찰: v1 스위트에 flaky 테스트가 하나 있다(2회 중 1회 실패, 재실행 시 4,301개 전부 통과). 동결 트리라 쫓지 않았고, 이제 v1 잡이 필터 뒤에 있어 자주 뜨지도 않는다.

### P1 — 코드 수술 (지금 착수)

| # | 항목 | 근거 | 크기 |
|---|---|---|---|
| 4 | **`NotesOutline.tsx` 갓 컴포넌트 분해.** 626줄 함수 하나에 훅 37개 호출. 1차로 클립보드 정책(`:293-416`, ~125줄)을 `useOutlineClipboard(...)`로, 커서 인계(`:263-292`)를 `useOutlineCaretHandoff()`로 추출한다. 둘 다 클로저 의존이 얕아 그대로 떼어진다. | `NotesOutline.tsx:55-680` — 단독 테스트 불가, 모든 아웃라인 기능이 이 파일에 착지 | M |
| 5 | **위치 인자 17~18개를 옵션 객체로.** `handleOutlineKeyDown`(17개)과 `handleImagePrimaryKeyDown`(18개)에는 맨몸 `() => void` 콜백이 4개, `NoteView[]`가 2개, `OutlineIndex`가 2개 있어 짝끼리 뒤바뀌어도 컴파일이 통과한다. 옵션 객체 선례는 같은 자리에 이미 있다 — `handleImageNodeKeyDown`이 객체 리터럴로 불린다. (1차 문서의 "18~19개"는 하나씩 과다 계수였다.) | `outlineSupport.ts:55-72`, `:163-181`; 호출부 `OutlineRow.tsx:420`, `:331` | S |
| 6 | **뷰포트 CTE 개선.** 80행 창 하나를 계산할 때마다 페이지 전체 서브트리를 재귀 CTE로 걷는다(문서 끝까지 스크롤하면 O(N²/80)). 재귀 조건이 `deleted = 0`뿐이라 `collapsed` 가지도 전부 내려간다. 접힌 가지 가지치기가 1순위. **정렬 키 물질화는 스키마 변경이라 §8 스키마 창으로 넘긴다.** 아래 제약 참조. | `crates/notes-sqlite/src/queries.rs:103-137`, `:359` — 출하 앱의 핵심 읽기 경로 | M |
| 7 | **읽기 경로의 쓰기 제거.** 뷰포트 조회마다 `notes_ui_state` upsert가 실행돼 스크롤이 단일 워커의 쓰기 트랜잭션 뒤에 줄을 선다. 아래 제약 참조 — 이 쓰기는 지울 수 없다. | `queries.rs:161-168` | S |
| 8 | **`validate()`에 자식 인덱스 도입.** `validate()`가 노드를 전부 부모 후보로 돌면서 `ordered_children`을 부르고, 그 안에서 매번 전체 값 스캔 + 정렬을 한다(O(n² log n)). 5k 노드면 약 2,500만 비교. `parent_id`로 한 번 그룹핑하면 끝난다. **`plan()`/`apply()`의 `self.clone()`은 건드리지 않는다** — 1차 문서는 이걸 낭비로 봤지만 `plan()`은 diff 상대가 필요하고 `apply()`는 검증 실패 시 롤백이 필요해서, 복사가 곧 그 의미다. | `crates/notes-core/src/tree.rs:28`, `:326`, `:355-366` | S |
| 8b | **`subtree_ids`·`visible_subtree_ids`의 재스캔 제거.** P1-8을 하다가 드러난 항목이다. `validate()`를 고쳐도 5k 서브트리 삭제는 여전히 느리다 — 이 두 함수가 pop한 id마다 전체 노드 값을 다시 훑는다(`visible_subtree_ids`는 거기에 정렬까지 얹는다). P1-8과 같은 부모 인덱스로 같은 모양의 수정이다. 이걸 안 하면 P1-8의 명분이던 "5k 서브트리 삭제"가 절반만 해결된 채 남는다. | `crates/notes-core/src/tree.rs:234-247`, `:249-259` | S |
| 9 | **폴더 구조 도입.** 178개 파일이 한 디렉터리에 평탄하게 있다. 이미 지켜지는 접두사(`store*` `outline*` `preview*` `image*`)가 그대로 폴더 이름이다. 기계적 이동이고, 옮기면 계층을 import 규칙으로 강제할 수 있다. **P1 안에서 맨 마지막** — 178파일 이동은 어떤 진행 중 브랜치와도 충돌한다. | `apps/desktop/src` 평탄 구조; lint가 사실상 꺼져 있음(`eslint.config.js:4-12`) | M |

**P1-6이 건드릴 수 없는 것 — 접힌 노드의 삼각형.** 프런트는 `hasChildren`을 평탄 배열에 자식이 들어 있는지로만 판정한다(`outlineIndex.ts:45`, 쓰는 곳은 `NotesOutline.tsx:648`과 `outlineKeyboard.ts:557`). `NoteView`에는 자식 수 필드가 없다(`packages/contracts/generated/NoteView.ts`). 그래서 접힌 서브트리를 서버에서 통째로 잘라내면 접힌 노드의 펼치기 삼각형이 사라지고 키보드 펼치기도 죽는다. 안전한 경계는 **접힌 노드의 직속 자식 한 겹까지는 내려가고 그 아래로는 멈추는 것**이다. 화면에 보이는 행만 정확한 `hasChildren`이 필요하고, 접힌 노드 아래 행은 어차피 렌더되지 않으므로 한 겹이면 충분하다. 통째로 자르려면 `NoteView`에 자식 수를 넣는 계약 변경이 따라와야 하니 P1 범위가 아니다.

**P1-6의 더 큰 위험 — 잘라내기 페이로드의 완전성.** 위 한 겹 타협으로도 안전하지 않을 수 있다. `cutImageNode`는 페이로드를 `store.getSnapshot()`에서 만들면서 `structuralContextComplete`(커서 양쪽이 null = 아웃라인 전체 로드)만 보고 안전하다고 판단한다(`outlineClipboardActions.ts`). 가지치기를 넣으면 커서가 양쪽 다 null이어도 스냅샷에 접힌 노드의 손자가 없다. 그러면 접힌 서브트리를 잘라낼 때 페이로드는 한 겹만 담고 삭제는 서버가 가진 전체를 지운다 — 되돌리거나 붙여넣어도 안 돌아오는 행이 생긴다. 대역 선택 경로는 `queryForest`(별도 IPC, 완전한 서브트리)를 쓰므로 무사하지만 이 경로는 아니다. `anchor_offset`(`queries.rs:357`)도 같은 CTE를 도는데, 둘의 가지치기 규칙이 한 글자라도 다르면 오프셋이 어긋나 창이 밀린다.

즉 P1-6은 자족적인 SQL 수정이 아니라 계약 층까지 걸친 변경이다. 착수 전에 이 세 가지를 코드로 검증하고, 하나라도 성립하면 가지치기는 §8 스키마 창으로 미룬다 — 거기서는 물질화된 경로 컬럼이 visible 플래그를 함께 지고 `NoteView`에 자식 수를 넣을 수 있어 안전하게 자를 수 있다. §6이 사각지대로 지목한 심부 스크롤 성능 계약 테스트는 그와 무관하게 지금 넣는다.

**P1-7이 지울 수 없는 것 — 재시작 복원.** `queries.rs:164`의 `active_page_id` 쓰기는 장식이 아니다. `bootstrap`이 `queries.rs:41`에서 이 값을 읽어 마지막으로 보던 페이지를 되살리고, 이게 parity matrix의 "restart restore"다. 유일한 writer가 이 upsert 하나뿐이고 `crates/notes-sqlite/tests/viewport_queries.rs:495`가 행동을 고정하고 있다. 1차 문서의 "디바운스하거나 세션 종료로 옮긴다"는 크래시 때 마지막 페이지를 잃는다. 올바른 최소 수정은 **값이 실제로 바뀔 때만 쓰는 것** — 같은 페이지를 스크롤하는 동안에는 쓰기가 0회, 페이지를 옮길 때만 1회.

P1-9 시점 재검토: 1차 문서는 "태그 작업 진행 중"을 근거로 이 항목을 태그 뒤로 미뤘다. 실제로는 태그 스펙만 커밋됐고 구현(자동완성·제자리 필터)은 시작 전이며 브랜치도 없다. 따라서 **지금이 충돌 없이 옮길 수 있는 가장 넓은 창**이고, 태그 구현은 새 구조 위에서 시작하는 편이 낫다.

### P2 — 기회 있을 때

| # | 항목 | 근거 |
|---|---|---|
| 10 | 서브트리 판정 5중 구현 통합 (`OutlineIndex`로 수렴). `subtreeIds`가 반환 타입이 다른 채 두 번 export된다. | `storeState.ts:17`, `outlineDragEngine.ts:134`, `outlineIndex.ts:96-108`, `useOutlineSelection.ts:109-122`, `outlineClipboard.ts:15-34` |
| 11 | 수제 롤백 5종을 역패치 공유로. 범용 역패치가 `previewPatchHistory.ts`에 이미 있다. | `storeOutlineMutations.ts:96-341`, `ponytail:` 표시 `:276` |
| 12 | preview 백엔드 유지 정책 결정. 브라우저 개발용으로 Rust 백엔드를 TS로 1,292줄 재구현(+테스트 1,074줄)했고 Rust 명령이 바뀔 때마다 두 벌을 수정한다. 유지한다면 두 구현의 드리프트를 잡는 계약 테스트가 필요하다. | `previewApi.ts` + `preview*` 8모듈 |
| 13 | 렌더 중 변이 3곳 정리. 앞의 둘은 지금도 제거 가능하고 `rowRuntime`은 성능 부하를 지는 축이라 문서화 후 유지한다. | `App.tsx:216`, `NotesOutline.tsx:140`, `:437` |
| 14 | 문서 드리프트 정리. parity matrix가 Export(`:51`)·Appearance(`:19`)를 "missing"이라 하지만 둘 다 출하됐다. README는 v1을 제품이라 설명하고 존재하지 않는 Monaco 표면을 언급한다. 계약 파일 수(32→37)와 번들 예산(300→312KB)도 낡았다. `architecture.md`에는 `crates/notes-export`가 빠져 있다. | 문서 9곳 대조 |
| 17 | `tree.rs`의 부모 그룹핑 두 벌을 한 벌로. P1-8이 `validate()` 안에 인라인 그룹핑을 넣고 P1-8b가 `children_index`를 넣어, 같은 파일에 같은 그룹핑이 두 번 있다. `children_index`가 `Vec<&NoteNode>`를 돌려주면 `validate()`가 그대로 쓰고 walker만 id로 매핑하면 된다 — 인라인 쪽 8줄이 사라진다. 리뷰를 통과한 두 커밋을 마지막에 손대지 않으려고 미룬 것이지 난도 때문이 아니다. | `crates/notes-core/src/tree.rs:355`(인라인), `:248`(`children_index`) |
| 18 | v2 엔트리 청크 목표치를 정하고 쪼갠다. 지금 엔트리가 324KB raw / 98KB gzip 한 덩어리고, P0-2가 그 크기를 예산으로 공식화했다. 지연 청크 24개 중 7개만 예산을 건드릴 만큼 크고 나머지 17개는 각 3.3KB 미만이라 잘못 eager import해도 안 걸린다. 크기 자체가 맞는지는 제품 판단이라 P0-2 범위 밖으로 뒀다. | `scripts/checkV2BundleBudget.mjs` |
| 15 | 저장소 위생. `.superpowers/sdd/` 추적 파일 24개를 `git rm --cached`(ignore가 소급 적용되지 않은 상태), `.claude/launch.json`의 죽은 절대 경로 제거, `packages/contracts`를 상대 경로 대신 패키지 이름으로 import. | 워크스페이스 별칭이 장식으로만 존재 |

### 결함이 아닌 것 (기록용)

**이미지 중복 검사의 재독은 결함이 아니다.** 1차 문서는 이 재독을 P2 항목으로 올렸다(`image_assets.rs:76-118`, "sha256이 이미 같음을 증명했는데 다시 읽는다"). 전제가 틀렸다. 해시는 방금 받은 **새** 바이트에서 계산했고, 디스크에 이미 있는 **기존** 파일이 그 내용과 같다는 증명은 어디에도 없다. 그 재독 + byte 비교가 기존 자산의 손상(잘림, 비트 부패, 외부 도구의 덮어쓰기)을 잡는 유일한 지점이고, 불일치면 조용히 넘기지 않고 에러를 낸다. 제거하면 손상된 파일 위에 새 노드를 얹는다. 20MB 재독이 실제로 문제가 되면 전체 비교 앞에 파일 크기 비교를 두는 정도가 상한이다.

`user_version != 1`이면 하드 에러를 내는 무마이그레이션 구조(`crates/notes-sqlite/src/schema.rs:78-82`)는 릴리즈 전 스키마 동결이라는 기존 결정과 일치한다. 다만 **첫 릴리즈 직전에** 빈 마이그레이션 사다리 골격을 넣는 일은 그 시점의 필수 항목으로 잡아 둔다(§8). 사다리 없이 릴리즈하면 다음 스키마 변경이 기존 설치를 전부 벽돌로 만든다.

`checkV2Architecture.mjs`가 내는 500/800줄 초과 경고 15건은 advisory여서 아무도 실패시키지 않는다. 목록이 곧 P1-4/P1-9 대상이다(`outlineKeyboard.ts` 745줄, `NotesOutline.tsx` 726줄, `App.tsx` 592줄, `outlineSupport.ts` 578줄, `notesStore.ts` 516줄).

## 4. 파일 SSOT 정합성 — Logseq 교훈과 포팅 계획

> **2026-08-14 전제 교체:** 이 절은 "단일 기기 데스크톱이 기본"을 해자 논거로 썼는데, 제품 목표가 **멀티 디바이스**로 확정됐다. v1 sync는 애초에 멀티 디바이스용으로 설계·구현돼 있었으므로(스펙 §0) 포팅 자산 목록은 유효하다. 확장된 계획 — 업계 조사, 아키텍처 선택, 워커 접합, undo 배리어, 마일스톤 M0~M6 — 은 [파일 SSOT 포팅 계획](2026-08-14-file-ssot-multi-device-port.md)이 이어받는다. 아래 미해결 4건 중 1번(undo×병합)과 4번(증분 재색인)의 답도 그 문서 §3.3·§6에 있다.

parity matrix에서 "Vault Markdown 동기화"(`feature-parity-matrix.md:56`)는 승인된 제외 항목이었다. 그 이월이 이제 만기다. 지향점을 유지하는 한 v2에 동기화 파이프라인이 없는 현 상태는 제품이 지향과 다른 저장 모델로 굳어 가는 중이라는 뜻이고, 데이터가 쌓일수록 전환 비용이 커진다.

### Logseq가 파일을 버린 이유가 우리에게 유리한 이유

Logseq는 2026-07 "Logseq 2.0"에서 파일 기반(OG)을 유지보수 모드로 내리고 SQLite를 정본으로 삼았다. 그런데 그들이 밝힌 실패 원인을 뜯어 보면 파일 자체가 아니라 설계 실수 쪽이다.

- 정체성이 본문 텍스트 안에만 있었다. `id::` 속성이 사용자 파일을 오염시키면서도 외부 편집 한 번에 블록 정체성이 깨졌다.
- 다중 기기 동시 쓰기가 데이터 손실의 실제 진원지였다. 여러 클라이언트가 같은 파일을 통째로 재작성했다.
- 블록 단위 편집을 파일에 직결해 키 입력마다 파일을 재작성했고, 시작할 때 전체를 재파싱해서 2천 페이지에 4~10분이 걸렸다.
- 그 결과 grep·git·"LLM에게 노트 폴더를 보여 주는" 워크플로를 잃었고, Markdown Mirror(단방향)와 CLI라는 별도 기능으로 재건축해야 했다. DB에서 내보낸 markdown은 구조적으로 손실이 있다고 스스로 문서화했다.

우리 설계는 이 함정을 대부분 비켜 간다. 단일 기기 데스크톱이 기본이라 동시 쓰기 문제가 없고, SQLite 캐시가 편집 입도를 흡수해 디바운스된 원자적 쓰기만 파일에 닿으며, 파일이 정본이므로 충실도는 구성상 보장된다(캐시는 언제든 버리고 재구축). Logseq가 기능으로 되사야 했던 것들 — grep, git, 파일시스템이 곧 API — 을 우리는 공짜로 갖는다. 이게 이 앱의 해자다.

### 미해결 설계 문제 4건 (포팅 착수 전에 답이 나와야 한다)

1차 문서는 이 마일스톤을 "포팅"이라 부르고 크기를 비웠다. 그 단어가 신규 설계 4건을 감추고 있다.

1. **되돌리기와 외부 편집의 화해.** 되돌리기 스택은 Rust가 소유하고 DB 상태를 되돌린다. watcher가 외부 파일 편집을 병합한 뒤, 스택에 이미 쌓인 항목들은 어떤 상태로 되돌아가는가? 사용자가 손대지 않은 편집을 되돌리기가 지워 버리는 경로가 생긴다. **이 항목이 SSOT 마일스톤의 최대 난제이고 1차 문서에 아예 없었다.** 후보: 외부 병합이 들어오면 스택을 자르기, 병합 지점에 배리어를 두기, 되돌리기를 병합과 같은 LWW 규칙 아래 넣기. 셋 다 사용자에게 보이는 행동이 다르니 결정이 필요하다.
2. **정체성 주석의 관용적 재대응.** v1 파일 포맷은 노드 정체성을 `<!-- yid: uuid t: hlc -->` 주석으로 파일 안에 둔다. Logseq의 `id::`와 같은 지점이다. 외부 편집기가 이 주석을 지우거나 망가뜨릴 때 텍스트 유사도나 위치로 기존 id에 다시 잇는 경로를 명시적 요구사항으로 넣는다. Logseq는 이게 없어서 정체성이 깨졌다.
3. **충돌 로그의 읽기 경로.** v1은 `merger.rs:2048`에서 로그를 쓰기만 하고 IPC로도 UI로도 노출하지 않아, 사용자가 덮어쓰인 편집을 복구할 데이터가 묻혀 있었다. 게다가 정리 경로가 없어 무한히 쌓인다. "신경 안 써도 잘 동작하는 앱"이라면 충돌은 조용히 기록하되 필요할 때 꺼내 볼 수 있어야 한다.
4. **증분 재색인의 정확성 기준.** mtime/hash 기반이 원칙인데, mtime이 거짓말하는 경우(클라우드 동기화 폴더, 시계 역행)의 안전망을 정한다. v1의 60초 전체 재스캔이 그 자리였지만 대형 vault에서 비용을 재봐야 한다.

### 포팅 자산과 순서

v1 파이프라인은 스텁이 아니라 완성돼 있고 이례적으로 엄격하다. SQL 트리거 기반 HLC 스탬핑·dirty 추적(`src-tauri/src/notes/schema.rs:208-228`), 3초/30초 디바운스, 렌더→재파싱 자가 검증, fsync + NOREPLACE 원자적 쓰기, notify 감시자 + 60초 전체 재스캔 안전망, 노드 단위 HLC LWW 병합(고아·순환 파킹 포함), 90일 tombstone GC, sha256 자산 GC를 모두 갖췄다. v2의 육각형 구조에 맞춰 `notes-sync` 크레이트로 옮긴다.

크기: **L (여러 주).** 위 4건이 신규 설계이고, 스키마 추가(dirty 추적, HLC 스탬프)가 필요하며, 포팅 대상이 30k줄이다. P0/P1의 S/M과 같은 축으로 읽으면 안 된다.

1. `file_io.rs` 원자적 쓰기 계층. 그대로 이식 가능한 최상급 자산이다.
2. exporter + 자가 검증. 이식하면서 알려진 결함을 함께 고친다 — `exporter.rs:262`의 dirty 노드당 N+1 조회(1,000노드 편집이면 약 3,000 추가 질의, 디바운스 만료까지 1Hz로 반복 평가)와 `runtime.rs:776`의 1초 폴링.
3. watcher + 부트스트랩 조정. 단 **시작 시 전체 재파싱 금지** — mtime/hash 기반 증분 재색인을 원칙으로 삼는다(미해결 4번). Logseq 최대 성능 실패 지점이다.
4. HLC LWW merger + 충돌 로그 읽기 경로(미해결 3번) + 되돌리기 화해(미해결 1번).

포팅 시 함께 확인할 v1 `ponytail:` 표시 7건: `exporter.rs:1713`(잠금 아래 세그먼트 쓰기), `repository.rs:308`(테스트가 레거시 vault 경로 유지), `watcher.rs:574`(병합과 회수 I/O 미분리), `runtime.rs:776`(폴링), `asset_gc.rs:34`(24시간 하드코딩), `asset_gc.rs:3146`(해시 불일치가 치명적), `merger.rs:1665`(병합 topic 서브트리만 재스캔).

또 하나: `src-tauri/src/notes/mod.rs:19`의 `#[allow(dead_code)]`가 30k줄 `sync` 모듈 전체를 덮고 있고 주석은 "Phase 1 wires pure file-format types ahead of the later runtime phases"라 주장하지만 실제로는 `lib.rs:36-37`이 명령을 등록해 쓰고 있다. v1에서 진짜 죽은 코드를 찾는 유일한 lint를 막고 있으므로, 포팅 범위를 잡기 전에 이 속성만 떼고 경고를 확인하는 편이 좋다.

## 5. Workflowy · Logseq DB 비교 — 기능 보강 우선순위

철학이 "간결한 화면, 필요할 때만 등장, 알아서 잘 동작"이므로 기준은 기능 수 경쟁이 아니라 감각 재현이다.

| Workflowy를 정의하는 것 (중요 순) | v2 상태 | 비고 |
|---|---|---|
| 1. 줌 + 브레드크럼 + 하나의 무한 트리 | 있음 | 완성. 분할 창은 Workflowy에 없는 우리 쪽 우위 |
| 2. 입력 즉시 필터되는 검색, **조상 사슬 보존** | 부분 | 사이드바 검색 패널은 있으나 아웃라인 제자리 필터가 아니다. **최대 격차** |
| 3. 키보드 모델 (Tab/Shift+Tab, 이동, 완료, 복제) | 있음 | 순수 해석기 + 테스트 1,234줄로 탄탄 |
| 4. 안 보이는 크롬 — 호버에만 나타나는 조작부 | 있음 | 앱 철학과 정확히 일치. 유지 |
| 5. 키 입력 단위 저장 + 깊은 undo | 있음 | Rust 소유 undo 스택. 파일 SSOT + git이 붙으면 Workflowy가 못 하는 세션 간 이력까지 가능 — 차별화 기회 |
| 6. 태그 — `#` 자동완성 + 클릭 즉시 필터 | 진행 중 | `docs/v2/tag-spec.md`가 정확히 이 둘을 설계. 토크나이저·인덱스·일괄 편집은 이미 출하됨. 남은 것은 자동완성 + 제자리 필터 |
| Cmd+K 퍼지 점프 팔레트 | 없음 | "감춰 놓되 필요하면 등장" 철학의 정수. 검색 다음 순위 권고 |
| 날짜 객체 · 캘린더 뷰 | 부분 | `notes_dates` 테이블은 이미 있고(`schema.rs:175`) UI만 없다. 후순위 |
| 미러 · 보드 뷰 · 표 | 없음 | **보류.** 미러는 파일 SSOT 포맷과 충돌하는 난제다(한 노드가 두 파일에?). SSOT 안착 전 착수 금지 |
| 전역 빠른 캡처 (핫키 → 인박스) | 없음 | 철학에 잘 맞는 후보. 작고 효과 크다 |

### Logseq DB에서 가져올 것과 버릴 것

- **버릴 것: 타입 속성·온톨로지·질의 언어.** Logseq DB의 간판 기능이지만 전부 "화면은 간결하게, 사용자는 신경 끄게"와 반대 방향이다. Workflowy가 이걸 전부 안 하고도 제품인 이유가 우리 철학의 근거다.
- **가져올 것: 태그를 본문 텍스트 밖에 렌더링하는 절충.** 의미는 부여하되 문법 소음은 없앤다. 현 tag-spec이 픽셀 불변 원칙 때문에 pill 렌더링을 보류한 결정과 공존 가능한 장기 참고안이다.
- **가져올 것: 정체성과 undo는 DB 계층에서.** undo를 파일 diff로 만들지 않는다(Logseq가 실명한 실패 지점). 우리 구조는 이미 이렇게 돼 있으니 유지한다 — 단 §4 미해결 1번이 이 결정의 대가다.
- **기록해 둘 것: 파일 접근성은 이제 시장의 기본 기대치다.** Logseq 사용자 이탈 사유 1위가 "일반 텍스트 포기"였고 "LLM에게 노트 폴더를 보여 준다"가 표준 워크플로가 됐다. 파일 SSOT 완성은 기능이 아니라 포지셔닝이다.

## 6. 성능 기준선

경쟁 제품의 실측 약점이 목표선을 정해 준다. Workflowy는 Electron 데스크톱이 대형 계정에서 버벅이는 것이 가장 약한 고리고(5만+ 항목대 불만 다수), Logseq 파일판은 시작 시 전체 재파싱으로 무너졌다.

| 지표 | 현재 계약 (`crates/notes-sqlite/tests/performance.rs`) | 권고 목표 | 근거 |
|---|---|---|---|
| 부트스트랩 50k 노드 | < 2s | SSOT 포팅 전까지 유지 | 포팅 후에는 이 계약이 **재는 대상 자체가 바뀐다**(파일 스캔 포함 여부). 그때 계약 문구를 다시 쓴다 |
| 5k 뷰포트 p95 | < 100ms | 유지 | — |
| 심부 스크롤 (P1-6에서 신설) | 5k 마지막 창 < 100ms · 50k 최심부 창 < 300ms | 페이지 크기와 무관하게 | **릴리즈 측정: 5k는 창당 33~65ms, 50k 최심부 창은 92.9~94.9ms.** 창 비용이 스크롤 깊이가 아니라 페이지 크기를 따라간다 — 창마다 서브트리 전체를 다시 걷기 때문이다. 50k에서 100ms 예산의 **95%를 쓴다**. 넘지는 않았지만 한 치수 큰 문서면 넘는다. 정렬 키 물질화(§8)가 창을 범위 스캔으로 바꿔야 끝난다 |
| 검색 (10만 노드) | 계약 없음 | < 100ms | Workflowy 데스크톱의 최약점. FTS5는 이미 있으므로(`schema.rs:183`) 계약 테스트만 추가 |
| 키 입력 → 화면 반영 | 낙관적 반영으로 즉시 | 유지 | draft 오버레이 구조가 이미 보장. 회귀 감시만 |
| 시작 시 파일 재파싱 (SSOT 포팅 후) | — | 변경분만 | mtime/hash 증분. Logseq 붕괴 지점의 직접 교훈 |

성능 계약은 `--features bench-fixtures`로 옵트인이라 평시 `cargo test --workspace`가 건너뛴다. P0-2의 결정 2번이 이 처분을 다룬다.

## 7. 권고 로드맵

1. **P1 구조 수술** (4 → 5 → 8 → 7 → 6 → 9). 사용자 지시로 지금 착수. 앞의 다섯은 서로 파일이 겹치지 않고, 9는 178파일을 옮기므로 맨 마지막이다. 진행은 §9.
2. **게이트 복구** (P0-2). CI를 v2로 올리고 그 안의 결정 3건을 함께 처리한다. P1 뒤로 놓는 이유는 P1-9가 경로를 전부 바꿔서 워크플로 파일을 두 번 쓰게 되기 때문이다.
3. **태그 작업 완주.** tag-spec의 자동완성 + 제자리 필터가 Workflowy 최대 격차를 직접 메운다. 새 폴더 구조 위에서 시작한다.
4. **스키마 창 정리** (§8). 릴리즈 전 마지막 기회에 스키마를 건드리는 일을 한 번에 몰아 넣는다.
5. **파일 SSOT 포팅 마일스톤** (L). `notes-sync` 크레이트로 v1 파이프라인 이식(§4 순서). 미해결 설계 4건을 먼저 정리한다. 이 앱의 정체성이 걸린 항목이라 태그 이후 최우선 대형 작업으로 권고한다.
6. **v1 아카이브 실행** (P0-3). **SSOT 포팅이 끝난 뒤**에 태그 + 브랜치 보존 후 main에서 제거한다. 1차 문서는 이걸 2순위로 뒀는데, 포팅 원본 30k줄을 포팅 전에 main에서 지우면 이식 내내 태그 체크아웃으로 오가야 하고 v1을 실행해 행동을 비교하는 oracle 경로도 불편해진다. 동결 코드는 아무도 만지지 않으니 부피 8할 제거가 급할 이유가 없다.
7. **감각 보강.** 검색 제자리 필터(조상 보존) → Cmd+K 점프 팔레트 → 전역 빠른 캡처. 셋 다 "감춰 놓되 필요할 때 등장" 철학의 직계 후손이다.
8. **보류 목록 유지.** 미러·보드·표·타입 속성은 SSOT 안착과 위 감각 보강이 끝날 때까지 착수하지 않는다.

## 8. 스키마 창 — 릴리즈 전에 한 번에 몰 것

`schema.rs:78-82`가 `user_version != 1`을 하드 에러로 막는다. 즉 스키마를 바꾸는 모든 일은 첫 릴리즈 전에 끝나거나, 마이그레이션 사다리를 먼저 깔아야 한다. 1차 문서는 이 항목들을 서로 다른 절에 흩어 놓아 의존 관계가 보이지 않았다. 한 흐름으로 묶는다.

| 스키마를 건드리는 일 | 어디서 왔나 |
|---|---|
| 뷰포트 정렬 키 물질화 | P1-6에서 분리 (접힌 가지 가지치기만 P1에서 하고, 물질화는 여기로) |
| dirty 추적 + HLC 스탬프 컬럼·트리거 | SSOT 포팅 (§4) |
| 빈 마이그레이션 사다리 골격 | 릴리즈 직전 필수 항목 |

순서: 사다리 골격을 **먼저** 깐다. 그러면 나머지 둘의 순서 제약이 풀리고, 릴리즈 후 스키마 변경도 벽돌을 만들지 않는다.

## 9. 실행 기록 — P1

항목마다: Opus xHigh가 TDD로 구현 → Fable xHigh 적대적 리뷰 → 필요하면 재작업 → 커밋 1개.

작업 브랜치: `refactor/p1-structure-surgery` (main에서 분기).

| # | 항목 | 상태 | 커밋 | 비고 |
|---|---|---|---|---|
| 4 | NotesOutline 분해 | **완료** | `b9a4c3f4` | 726→585줄. `outlineCaretHandoff.ts`(37줄) + `outlineClipboardActions.ts`(172줄)로 분리, 단위 테스트 22개 추가. 리뷰 1라운드 pass + minor 1건(대역 복사 거부 경로 미검증) 즉시 보강 |
| 5 | 옵션 객체 전환 | **완료** | | `outlineSupport.ts` 579→544줄. 공유 `OutlineRowKeyContext` 15필드 + 각자 확장 2개. `executeRowIntent`(같은 위험을 지닌 10인자)도 함께 전환 — 범위 확장이지만 호출부가 전환 대상 둘뿐이라 diff가 오히려 줄었다. 직접 테스트 18개 신규, 뒤바뀜 7종을 전환 전후 두 번 측정해 잡히는 것과 못 잡는 것을 기록. 리뷰 findings 0건 |
| 8 | validate 자식 인덱스 | **완료** | | 부모별 그룹핑 한 번으로 교체(+12/−5줄). 5k 노드 `plan()` 3.30s → 0.04s. 통합 테스트 5개 신규, 그중 스케일 테스트는 되돌리면 실제로 실패함을 확인. 삭제된 형제를 포함하는 순서 검사를 그대로 보존(살아 있는 노드만 인덱싱하면 통과해 버리는 구멍을 테스트가 막는다). 리뷰 findings 0건 |
| 7 | 읽기 경로 쓰기 제거 | **완료** | | 저장값이 바뀔 때만 쓰도록 가드. 같은 페이지 스크롤 20회 기준 쓰기 20→0. 결정적 근거는 `BEGIN IMMEDIATE`로 쓰기 잠금을 잡고 측정한 것 — 가드 전 5.175s(busy_timeout 만료), 후 480µs. 무경합 구간의 벽시계는 구분되지 않는다는 점도 함께 기록. `queries.rs`가 584줄이 되어 advisory 경고가 하나 늘었다(테스트 모듈 때문. 파일 분리는 이 저장소에 없는 패턴이라 받아들임). 리뷰 findings 0건 |
| 6 | 뷰포트 CTE | **완료 — 가지치기는 §8로 연기** | | 위험 3건 전부 성립. 특히 위험 B는 내가 짚은 것보다 넓었다 — 스냅샷에서 페이로드를 만들고 서버 서브트리를 지우는 경로가 `outlineClipboardActions.ts:148`, `outlineMenuCommands.ts:134`·`:330`, `useOutlineSelection.ts:125` 넷이고, `outlineClipboard.ts:133`의 클립보드 walk는 한계를 넘으면 조용히 자른다. `anchor_offset`의 CTE는 `viewport()`와 byte-identical. 따라서 SQL 층 안에서 닫히지 않는다. 대신 심부 스크롤 성능 계약을 신설(§6 표). 리뷰 2라운드 — 1라운드는 50k 절벽을 주석에만 적고 단언하지 않아 반려됐다 |
| 8b | subtree_ids 재스캔 제거 | **완료** | | 부모 인덱스를 한 번 만들어 걷도록 교체(+39/−21줄). 5k 서브트리 삭제 1.61s → 0.03s, 5k 서브트리 복제 1.71s → 0.07s. 둘 다 P1-8이 끝난 코드에서 잰 값이라 이 항목이 걷어낸 시간만 남는다. 통합 테스트 4개 신규 — 순서와 도달 범위를 고정한 2개는 수정 전 코드에서 먼저 초록임을 확인했고 스케일 2개는 수정 전 코드에서 실제로 실패한다. `subtree_ids`는 삭제된 행까지 걷고 `visible_subtree_ids`는 걷지 않는 차이를 그대로 뒀다. 달라진 지점은 `subtree_ids`의 방문 순서 하나다(id 순 → 문서 순). 호출부가 id마다 플래그만 세우는 `set_subtree_deleted` 하나라 순서를 보는 곳이 없다. 기존 커맨드 테스트 33개는 복제가 삭제 행을 건너뛰는지 검증하지 않아 인덱스에 삭제 행을 넣어도 전부 통과한다 |
| 9 | 폴더 구조 | **완료 — 별도 브랜치, 머지 대기** | `958f8b20` (`refactor/p1-9-folder-structure`) | 다른 세션이 같은 트리에서 desktop 파일을 편집 중이었으므로 격리 worktree(`.worktrees/p1-9-folders`)에서 수행했다. 120파일 이동(`outline/` 69, `image/` 23, `store/` 17, `preview/` 11), 60파일은 제자리. `apps/desktop/src` 아래 변경된 줄은 전부 import 지정자 하나뿐이고, rename 감지도 정상(순수 rename 23, rename+수정 97). tsc 초록, 게이트 초록, 786테스트 통과. **부수 소득이 이 항목의 실질 성과다 — 아래 가드 발견.** 리뷰 findings 0건 |

착수 전 게이트 기준선: `npm run test:v2` 전 단계 초록 (cargo workspace / vitest / lint / 아키텍처 / 계약).

### P1-9가 드러낸 것 — 레거시 차단 가드는 깊이 하나에서만 작동했다

`checkV2Architecture.mjs`가 v2 코드의 v1 프런트엔드 import를 막는 검사는 §1이 "별개 애플리케이션"이라 부르는 경계를 지키는 유일한 장치다. 그 검사가 `../../../src/`를 **문자열로** 매치하고 있었다. 즉 `apps/desktop/src` 바로 아래 파일에서만 작동하고, 파일이 서브폴더로 한 단계라도 내려가면 레거시 경로가 `../../../../src/`가 되어 조용히 통과한다.

폴더 이동이 이걸 실제 문제로 만들었으므로 고치기 전에 먼저 증명했다 — 옛 정규식 그대로 두고 서브폴더 파일에 v1 import를 넣으니 `v2 architecture boundaries PASS`가 나왔다. 지금은 지정자를 해석해 루트 `src/` 안에 떨어지는지 보므로 깊이를 따지지 않고, 두 깊이 모두에서 던진다.

교훈은 P0-2 결정 2번과 같다. 검사가 초록인 것과 검사가 무언가를 보고 있는 것은 다른 얘기다.
