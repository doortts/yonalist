# Task 3 Report — Phase 3 Bootstrap, Exporter, and Flush

## Status

DONE. Phase 3 adds startup reconciliation, deterministic SQLite-to-Markdown
export, durable dirty scheduling, and the native start/status/flush/stop runtime
surface. It intentionally does not add the Phase 4 watcher, `notify`, sync
events, frontend listeners, asset handling, or settings UI.

## Contract delivered

- Startup implements all four SSOT branches: new DB with/without existing
  Markdown, and existing DB with/without existing Markdown. Existing files are
  hash-compared, parsed, merged, or quarantined; empty vaults receive a full
  local export; crash-left dirty rows flush immediately.
- Topic filenames are assigned once from the first local/source title and never
  follow later title edits. `trash.md` uses the reserved non-UUID
  `__yonalist_trash__` metadata identity, so a normal topic cannot claim it.
- Topic and trash snapshots render deterministically, parse back into the exact
  semantic document, publish atomically, record SHA-256 echo hashes, then clear
  only the dirty markers whose captured HLC/fingerprint is still current.
- Dirty scheduling exports after 3 seconds idle or 30 seconds total. Failed
  targets remain scheduled; one failure does not starve healthy targets.
  Explicit flush and stop bypass debounce.
- Local empty-trash and history hard-delete paths record HLC purge tombstones
  before deleting rows. Trash rendering preserves independent deletion-batch
  roots and makes purge evidence durable before a retired topic file is removed.
- Root deletion, purge, restore, and cross-topic movement maintain their former
  and destination topic files. Removal waits for the dependency topic/trash
  export, retries parent-directory durability after an interrupted unlink,
  preserves externally changed source bytes by comparing them with the stored
  export hash, and recreates the original immutable filename on restore.
- Synthetic former-root removal markers are coalesced when the root returns by
  any path, including history/merge rerooting, avoiding stale removal failures.
- The trash lifecycle trigger is installed both for new databases and every
  open of an existing schema-v2 database. One shared purge-evidence helper owns
  tombstone/HLC persistence for repository and history deletion paths.
- `NotesSyncRuntime` is idempotent across repeated start/stop and vault switches.
  The four Tauri commands are registered and granted only to the local main
  window; generated ACL schemas reflect the command permissions.

## Files

- `src-tauri/src/notes/sync/bootstrap.rs`
- `src-tauri/src/notes/sync/exporter.rs`
- `src-tauri/src/notes/sync/runtime.rs`
- `src-tauri/src/notes/sync/mod.rs`
- `src-tauri/src/notes/schema.rs`
- `src-tauri/src/notes/repository.rs`
- `src-tauri/src/notes/history.rs`
- `src-tauri/src/file_io.rs`
- `src-tauri/src/lib.rs`
- `src-tauri/build.rs`
- `src-tauri/permissions/main-window.toml`
- `src-tauri/permissions/autogenerated/notes_sync_*.toml`
- `src-tauri/gen/schemas/{acl-manifests,desktop-schema,macOS-schema}.json`
- `.superpowers/sdd/task-3-report.md`

## TDD evidence

The initial RED cases failed on missing scheduler, bootstrap, and runtime
contracts. The first GREEN wave established the 3s/30s schedule, four startup
branches, deterministic publish, and command lifecycle.

Focused RED → GREEN regressions then covered:

- startup trash reconciliation, initial-export quarantine isolation, exact
  last-good quarantine recovery, and per-target bootstrap failure isolation;
- reserved trash identity, lifecycle-trigger upgrade for an existing v2 DB,
  local purge evidence, and independent deletion-batch boundaries;
- atomic failure, semantic self-validation failure, exact dirty clearing,
  failed debounce retry, and deterministic trash purge rendering;
- deleted/restored root retirement, dependency-before-remove ordering, unlink
  and post-unlink retry, purge-before-flush, and immutable filename recreation;
- direct and history-replayed cross-topic movement, history hard-delete purge
  evidence, root A→B→A coalescing, and merge/replay reroot coalescing;
- external source modification after the last export: RED removed the changed
  bytes and cleared dirty state; GREEN refuses removal and preserves both;
- runtime start/stop/vault-switch idempotence and force-flush behavior; and
- exact four-command ACL coverage.

Final focused result: `notes::sync` **161 passed, 0 failed**.

## Independent review

The review waves found and drove fixes for startup branch completeness,
reserved trash identity, failure isolation, old-v2 trigger installation,
hard-delete purge evidence, cross-topic/root retirement, durable unlink retry,
root round-trip coalescing, changed-source preservation, and path-independent
reroot coalescing. The two code-quality minors were also removed by making the
trigger installer and purge-evidence writer canonical shared helpers.

Final refreshed verdict: Critical/Important/Minor **0/0/0** and
**Implementation Ready: Yes**. The reviewer confirmed all lifecycle, purge,
former-topic, durability, crash-retry, external-edit, and reroot findings are
resolved, with no remaining documented-standard violation or actionable smell.

## Verification

- `cargo test notes::sync --lib` — pass: **161 passed, 0 failed**.
- `cargo test` — pass: **910 passed, 3 intentionally ignored, 0 failed**.
- `npm run lint` — pass.
- `npx tsc --noEmit` — pass.
- `npm test` — pass: **3,851 passed, 27 skipped** across 183 files
  (182 passed, 1 skipped).
- `npm run test:architecture` — pass; Notes workspace architecture budget PASS.
- `cargo fmt --all -- --check` — pass.
- `git diff --check` — pass.

An exploratory strict repository-wide Clippy run encountered the repository's
pre-existing warning baseline outside this task and is not part of the common
gate. Task-local warnings surfaced during that audit were fixed.

## Phase boundaries and residual risk

- There is no manual UI verification in Phase 3 because it adds no frontend
  listener or user-visible event surface.
- External edits are preserved during retirement and remain dirty for the Phase
  4 watcher/restart merger to consume.
- Filesystem watching, periodic scan, bounced-copy handling, 500ms frontend
  coalescing, `notes://sync-changed`, and asset-arrival behavior remain Phase 4.
- Asset deduplication/GC, progress events, and settings remain Phase 5.

## Sol final-review fix wave

The root Sol review reopened Phase 3 with three Important findings. The fix
wave remains within the Phase 3 Rust/SQLite boundary and adds no watcher,
event, frontend, asset, or settings behavior.

### Remote merge counterpart durability

Topic and trash mergers now capture lifecycle/topic ownership before applying
remote evidence and compare it with ownership immediately before commit. The
same merge transaction dirties every non-source counterpart changed by the
merge: former topic rewrite/removal, current recovery topic, or reserved Trash.
This does not rely on local lifecycle triggers, which explicit winning remote
HLC updates intentionally bypass. Bootstrap records the incoming source hash
only after the merge transaction—and therefore all counterpart markers—has
committed.

Focused RED → GREEN cases covered remote trash deletion, remote purge, remote
topic restore, cross-topic former-root movement, and a local win against stale
remote trash: **0/5 → 5/5**.

The ownership capture is bounded to affected node IDs plus their unique
ancestor closure, queried in SQLite-safe chunks and resolved with root
memoization. It does not scan the vault for every changed file. A regression
covering more IDs than SQLite's variable limit passes. Existing topic sources
are also dirtied after applied remote changes, while a metadata-absent root is
not mistaken for a retired file target.

### Purge-after-retirement ordering

The shared local purge-evidence helper now upserts the reserved Trash dirty
marker in the same transaction as purge tombstones and HLC persistence. A root
that was soft-deleted, fully exported/retired, and only later permanently
purged now republishes `trash.md` with its purge line before the absent-file
removal retry can clear the final marker.

The dedicated regression failed with `trash.md is not durably exported`, then
passed together with the prior local-purge regression.

### Retryable startup target failures

Bootstrap now treats individual file parse/merge and export failures as target
outcomes: it quarantines and reports the failed file, continues healthy files,
aggregates unique error messages, and still starts the exporter worker. Failed
initial fallback exports that had no prior dirty row receive a target-specific
retry marker; existing dirty failures retain their captured marker. Start and
status therefore expose running/quarantine/dirty state instead of converting a
single target failure into a runtime-start failure. Database opening, locking,
schema validation, and worker creation remain fatal setup boundaries.

Focused RED → GREEN cases covered an invalid file beside a healthy local
export, one merge/filename failure beside a healthy source file, and one
initial export failure beside a healthy topic with no preexisting dirty rows.
The last case initially started but reported zero retry targets; it now reports
one dirty target while the healthy file is present.

### Fix-wave verification

- `cargo test notes::sync --lib` — pass: **161 passed, 0 failed**.
- `cargo test` — pass: **910 passed, 3 intentionally ignored, 0 failed**.
- `npm run lint` and `npx tsc --noEmit` — pass.
- `npm test` — pass: **3,851 passed, 27 skipped** across 183 files
  (182 passed, 1 skipped).
- `npm run test:architecture` — pass; Notes workspace architecture budget PASS.
- `cargo fmt --all -- --check` — pass.
- `git diff --check` — pass.
- Independent frozen-diff standards/spec review — Critical/Important/Minor
  **0/0/0**, Implementation Ready: **Yes**.
