{
  "name": "@yonalist/desktop",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.ts",
    "build": "tsc -p tsconfig.json && vite build --config vite.config.ts",
    "test": "vitest run --config vite.config.ts"
  },
  "dependencies": {
    "@tauri-apps/plugin-dialog": "2.7.1",
    "monaco-editor": "^0.53.0"
  }
}
